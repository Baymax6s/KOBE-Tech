basePath: /
definitions:
  postlogin.ErrorResponse:
    properties:
      message:
        type: string
    type: object
  postlogin.LoginRequest:
    properties:
      name:
        type: string
      password:
        type: string
    required:
    - name
    - password
    type: object
  postlogin.LoginResponse:
    properties:
      token:
        type: string
    type: object
  server.articleErrorResponse:
    properties:
      message:
        type: string
    type: object
  server.articleJSONResponse:
    properties:
      content:
        type: string
      created_at:
        type: string
      id:
        type: integer
      title:
        type: string
      updated_at:
        type: string
      user_id:
        type: integer
    type: object
  server.createArticleRequest:
    properties:
      content:
        type: string
      title:
        type: string
    type: object
  server.listArticlesResponse:
    properties:
      articles:
        items:
          $ref: '#/definitions/server.articleJSONResponse'
        type: array
    type: object
host: localhost:8080
info:
  contact: {}
  description: KOBE-Tech API documentation generated from Go annotations.
  title: KOBE-Tech API
  version: 0.1.0
paths:
  /api/articles:
    get:
      description: Get article list API.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/server.listArticlesResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/server.articleErrorResponse'
      summary: List articles
      tags:
      - articles
    post:
      consumes:
      - application/json
      description: Create article API.
      parameters:
      - description: Create article request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/server.createArticleRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/server.articleJSONResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/server.articleErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/server.articleErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/server.articleErrorResponse'
      summary: Create article
      tags:
      - articles
  /auth/login:
    post:
      consumes:
      - application/json
      description: Authenticate user and return JWT token
      parameters:
      - description: Login credentials
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/postlogin.LoginRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/postlogin.LoginResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/postlogin.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/postlogin.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/postlogin.ErrorResponse'
      summary: Login
      tags:
      - auth
schemes:
- http
swagger: "2.0"
